# BioSpark-Light — local desktop app dependencies
#
# Trimmed vs. the web version:
#   - Dropped: onnxruntime, mne, pyedflib, neurokit2 (no inference / EDF support)
#   - Dropped: asyncpg, python-jose, bcrypt, email-validator (no Postgres / no auth)
#   - Added:   platformdirs (per-OS data dir), pystray + pillow (system tray icon)

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Numerics + ML
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0
scikit-learn>=1.3.0
torch>=2.0.0

# Plotting (matplotlib/seaborn power the publication figures + report)
matplotlib>=3.8.0
seaborn>=0.13.0

# Persistence (SQLite only — no Postgres driver)
sqlalchemy[asyncio]>=2.0.0
aiosqlite>=0.20.0

# Pydantic (v2)
pydantic>=2.0.0

# Desktop launcher
platformdirs>=4.0.0
pystray>=0.19.5
pillow>=10.0.0
